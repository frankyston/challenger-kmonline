<%= f.input :content%>

<%= f.fields_for :answers do |builder|%>
  <%= render 'answer_fields', :f => builder%>
<%end%>

<div class='links'>
  <%= link_to_add_association "Adicionar resposta", f, :answers, class: 'btn btn-success' %>
</div>
<br>
